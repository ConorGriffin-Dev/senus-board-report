pydantic>=2.9
sqlalchemy>=2.0
asyncpg>=0.29
python-dotenv>=1.0
ruff>=0.7
pytest>=8.3

# Only needed if the live-API swap point in extraction_service.py is wired
# up. Not required for fixture-based runs.
anthropic>=0.39

# Document parsing libraries are deliberately absent. Under the fixture
# method the model reads the source PDF directly, so nothing in this
# package parses documents. See assumption A-17.
